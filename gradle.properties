# Gradle properties
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
# org.gradle.java.home is intentionally omitted here to avoid hardcoding a machine-specific JDK path.
# Configure the local JDK using JAVA_HOME or the IDE build settings when needed.
